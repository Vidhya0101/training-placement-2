class Solution {
    public int countVowels(String s) {
        int c = 0;
        for (char ch : s.toLowerCase().toCharArray())
            if ("aeiou".indexOf(ch) != -1) c++;
        return c;
    }
}
