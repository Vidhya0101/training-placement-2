from collections import Counter
def intersect(nums1, nums2):
    c = Counter(nums1)
    res = []
    for n in nums2:
        if c[n] > 0:
            res.append(n)
            c[n] -= 1
    return res
