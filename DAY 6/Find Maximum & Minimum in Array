class Solution {
    public int[] findMinMax(int[] arr) {
        int min=arr[0], max=arr[0];
        for(int n:arr){
            min=Math.min(min,n);
            max=Math.max(max,n);
        }
        return new int[]{min,max};
    }
}
